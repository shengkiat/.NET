@model ActiveLearning.DB.Course

@{
    ViewBag.Title = "Delete Course";
}

<h2>Delete Course</h2>

@Html.Partial("_MessageBoxPartial")

@if (Model != null)
{
    <div class="container text-center">
        <h4 style="margin-top:100px">Confirm to delete ?</h4>
    </div>
    <hr />
    <div class="form-group table" style="margin-left:35%">
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.CourseName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.CourseName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.CreateDT)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.CreateDT)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.UpdateDT)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.UpdateDT)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-group" style="margin-left:45%">
                <input type="submit" value="Delete" class="btn btn-primary" />
            </div>
        }
    </div>
}
